<h1><%=t('.header', title: @test.title)%></h1>

<p>
<%= t('.level', level: @test.level)%>
</p>

<table>
      <tr>
        <th><%=t('.questions')%></th>
        <th colspan="3"> <%=t('.actions')%></th>
      </tr>

      

      <%= render @test.questions %>
      
    </table>

<p></p>

<%= link_to t('.create_new_question'), new_admin_test_question_path(@test) %> |
<%= link_to t('.edit_test'), edit_admin_test_path(@test) %> |
<%= link_to t('.delete_test'), admin_test_path(@test), method: :delete %> |
<%= link_to t('.back'), admin_tests_path %>