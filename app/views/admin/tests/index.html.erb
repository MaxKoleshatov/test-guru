<h1>Все тесты: </h1>
  <% if @tests.present? %>
    <table>
      <tr>
        <th>Название</th>
        <th>Уровень</th>
        <th>Количество вопросов</th>
        <th colspan="4"> Действия</th>
      </tr>

      <%= render @tests %>
    </table>
  <% else%>
    <p><%= render plain:"У вас нет тестов"%></p>
  <% end%>

  <%= link_to 'Создать новый тест', new_admin_test_path %><br>
  <%= link_to 'На главную', root_path %>
