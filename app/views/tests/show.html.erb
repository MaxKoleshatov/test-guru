<h1>Тест под названием  - "<%= @test.title %>" </h1>

<p>
Уровень сложности  - "<%= @test.level %>"
</p>

<table>
      <tr>
        <th>Вопросы</th>
        <th colspan="3"> Действия</th>
      </tr>

      <%= render @test.questions %>
    </table>

<p></p>

<%= link_to 'Создать новый вопрос', new_test_question_path(@test) %> |
<%= link_to 'Отредактировать тест', edit_test_path(@test) %> |
<%= link_to 'Удалить тест', test_path(@test), method: :delete %> |
<%= link_to 'Назад', tests_path %>